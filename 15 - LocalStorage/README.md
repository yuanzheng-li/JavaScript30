# Local Storage and Events Delegation

## Notes

### Element.matches()

The `Element.matches(selectorString)` method returns `true` if the element would be selected by the specified `selectorString`; otherwise, returns `false`.